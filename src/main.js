/* downsize.js main */

// Base function.
var downsize = function() {
  // Add functionality here.
  return true;
};


// Version.
downsize.VERSION = '0.0.0';


// Export to the root, which is probably `window`.
root.downsize = downsize;
